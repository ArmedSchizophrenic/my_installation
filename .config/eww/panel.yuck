(defwindow panel
  :monitor 0
  :geometry 
  (geometry
    ; :x "15px"
    ; :width "20%"
    ; :height "92%"
    ; :anchor "center right"
    :width "18%"
    :height "93%"
    :anchor "center right"  
  )
  (body)
)

(defvar collapse true)
(defwidget body []
(box :space-evenly false 
  (box :class "panel_items")
  (box :class "panel_items"
        )
  )  
)

;:width "20%"
;    :height "20%"
;    :valign "fill"
;    :halign "fill"
;    :hexpand true
  ;  (calendar)

